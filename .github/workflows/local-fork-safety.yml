name: local-fork-safety

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  safety:
    name: local fork safety
    uses: ./.github/workflows/reusable-local-safety.yml
